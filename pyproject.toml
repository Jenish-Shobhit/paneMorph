[project]
name = "panemorph"
version = "0.1.0"
description = "Move live Herdr panes between tabs without restarting their processes."
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Jenish Shobhit" }]

[tool.unittest]
start-directory = "tests"
